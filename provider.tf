provider "aws" {
  region = "${var.aws_region}"

  #access_key = "<your_aws_access_key>"
  #secret_key = "<your_aws_secret_key>"

  # default_tags {
  #   tags = {
  #     Name = "architect-demo"
  #   }
  # }
}